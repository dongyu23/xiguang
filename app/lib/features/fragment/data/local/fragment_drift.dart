class FragmentRow {
  const FragmentRow({
    required this.id,
    required this.contentText,
    required this.updatedAt,
  });

  final int id;
  final String contentText;
  final DateTime updatedAt;
}
